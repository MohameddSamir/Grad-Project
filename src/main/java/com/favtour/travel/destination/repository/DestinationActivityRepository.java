package com.favtour.travel.destination.repository;

import com.favtour.travel.destination.entity.DestinationActivity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DestinationActivityRepository extends JpaRepository<DestinationActivity, Integer> {
}
